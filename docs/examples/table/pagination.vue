<template>
  <bk-table :raw-data="tableData" :pagination="true" @pagination-change="handlePaginationChange">
    <bk-table-column prop="date" label="Date" width="180" />
    <bk-table-column prop="name" label="Name" width="180" />
    <bk-table-column prop="address" label="Address" />
  </bk-table>
</template>

<script setup lang="ts">
const tableData = Array.from({ length: 50 }, (_, index) => ({
  date: `2016-05-${String(index + 1).padStart(2, "0")}`,
  name: `User ${index + 1}`,
  address: `No. ${index + 1}, Grove St, Los Angeles`
}))

const handlePaginationChange = (page: number, size: number) => {
  console.log("Pagination change:", page, size)
}
</script>
